# Nixpacks 配置文件 - 指导 Railway 如何构建和运行项目

[phases.setup]
# 安装所有依赖（包括 workspaces）
cmds = ["npm install"]

[phases.build]
# 构建阶段（后端不需要构建，但我们确保 server 目录准备就绪）
cmds = ["echo 'Build complete'"]

[start]
# 启动命令 - 从根目录运行 server
cmd = "npm run server"
